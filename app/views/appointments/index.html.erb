<p id="notice"><%= notice %></p>

<h1>Lista Eventi</h1>

<table>
  <thead>
    <tr>
      <th>Titolo</th>
      <th>Creato il </th>
      <th>Aggiornato il </th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @appointments.each do |appointment| %>
      <tr>
        <td><%= appointment.title %></td>
        <td><%= appointment.created_at %></td>
        <td><%= appointment.updated_at %></td>
        <td><%= link_to 'Mostra', appointment %></td>
        <td><%= link_to 'Aggiorna', edit_appointment_path(appointment) %></td>
        <td><%= link_to 'Elimina', appointment, method: :delete, data: { confirm: 'Sei sicuro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuovo Appuntamento', new_appointment_path %>
